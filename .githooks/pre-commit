#!/bin/bash
# Pre-commit hook: コミット前の品質チェック

set -e

echo "🔍 Pre-commit checks starting..."

# 1. ステージングエリアのファイルをチェック
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)

if [ -z "$STAGED_FILES" ]; then
    echo "⚠️ No files staged for commit"
    exit 0
fi

# 2. Python ファイルのチェック
PYTHON_FILES=$(echo "$STAGED_FILES" | grep -E '\.py$' || true)
if [ -n "$PYTHON_FILES" ]; then
    echo "🐍 Checking Python files..."

    # Ruffチェック（存在する場合）
    if command -v ruff >/dev/null 2>&1; then
        echo "  Running Ruff linter..."
        ruff check $PYTHON_FILES || {
            echo "❌ Ruff check failed. Please fix errors before committing."
            echo "💡 Run 'ruff check --fix' to auto-fix some issues"
            exit 1
        }
    fi

    # Blackフォーマットチェック（存在する場合）
    if command -v black >/dev/null 2>&1; then
        echo "  Checking Black formatting..."
        black --check $PYTHON_FILES || {
            echo "❌ Black formatting check failed"
            echo "💡 Run 'black <files>' to format"
            exit 1
        }
    fi
fi

# 3. TypeScript/JavaScript ファイルのチェック
TS_JS_FILES=$(echo "$STAGED_FILES" | grep -E '\.(ts|tsx|js|jsx)$' || true)
if [ -n "$TS_JS_FILES" ]; then
    echo "📘 Checking TypeScript/JavaScript files..."

    # ESLintチェック（存在する場合）
    if [ -f "frontend/package.json" ] && [ -f "frontend/node_modules/.bin/eslint" ]; then
        echo "  Running ESLint..."
        (cd frontend && npx eslint $TS_JS_FILES) || {
            echo "❌ ESLint check failed"
            echo "💡 Run 'pnpm lint:fix' to auto-fix"
            exit 1
        }
    fi
fi

# 4. 大きなファイルのチェック（1MB以上）
for file in $STAGED_FILES; do
    file_size=$(stat -f%z "$file" 2>/dev/null || stat -c%s "$file" 2>/dev/null || echo 0)
    if [ "$file_size" -gt 1048576 ]; then
        echo "⚠️ Warning: $file is larger than 1MB ($(($file_size / 1024))KB)"
        read -p "Continue with large file? (y/n) " -n 1 -r
        echo
        if [[ ! $REPLY =~ ^[Yy]$ ]]; then
            exit 1
        fi
    fi
done

# 5. 秘密情報の漏洩チェック
echo "🔐 Checking for secrets..."
for file in $STAGED_FILES; do
    # API keys, tokens, passwords のパターンチェック
    if grep -qE '(api[_-]?key|apikey|secret|password|token|bearer|private[_-]?key)[\s]*[:=][\s]*["\047][^"\047]+["\047]' "$file" 2>/dev/null; then
        echo "⚠️ Potential secret found in $file"
        echo "Please review the file for sensitive information"
        read -p "Continue anyway? (y/n) " -n 1 -r
        echo
        if [[ ! $REPLY =~ ^[Yy]$ ]]; then
            exit 1
        fi
    fi

    # .envファイルのコミット防止
    if [[ "$file" == *".env"* ]] && [[ "$file" != *".env.example"* ]]; then
        echo "❌ Error: Attempting to commit .env file: $file"
        echo "💡 Use .env.example for template files"
        exit 1
    fi
done

# 6. TODO/FIXME チェック
TODO_COUNT=$(grep -n "TODO\|FIXME\|XXX" $STAGED_FILES 2>/dev/null | wc -l || echo 0)
if [ "$TODO_COUNT" -gt 0 ]; then
    echo "📝 Found $TODO_COUNT TODO/FIXME comments:"
    grep -n "TODO\|FIXME\|XXX" $STAGED_FILES 2>/dev/null | head -5 || true
    if [ "$TODO_COUNT" -gt 5 ]; then
        echo "   ... and $((TODO_COUNT - 5)) more"
    fi
fi

echo "✅ Pre-commit checks passed!"